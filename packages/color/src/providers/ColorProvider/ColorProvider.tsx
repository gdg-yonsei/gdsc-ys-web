import React, { Fragment } from 'react';

import type { ColorProviderProps } from './ColorProvider.type';

const ColorProvider = ({ children }: ColorProviderProps) => {
  //TODO: FILL ME!
  return (
    <Fragment>
      {children}
    </Fragment>
  )
}

export default ColorProvider;