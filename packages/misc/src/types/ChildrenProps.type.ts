import type { PropsWithChildren } from 'react';

/**
 * ### ChildrenProp
 *
 * Preset for component props typing when children is needed
 *
 * @abstract
 */
export interface ChildrenProp extends PropsWithChildren<{}> {}
